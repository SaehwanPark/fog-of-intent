[package]
name = "foi-protocol"
version = "0.1.238"
edition = "2024"
rust-version = "1.96"
license = "MIT"
description = "Model-agnostic actor-visible DTOs and MCP wire codecs for Fog of Intent"

[lints.clippy]
as_conversions = "deny"

[dependencies]
foi-kernel = { path = "../foi-kernel" }
foi-lane = { path = "../foi-lane" }
