[package]
name = "foi-agent"
version = "0.1.239"
edition = "2024"
rust-version = "1.96"
license = "MIT"
description = "Behavioral policies, empirical calibration, team dialogue, trust, and 4-quadrant debriefs for Fog of Intent"

[lints.clippy]
as_conversions = "deny"

[dependencies]
foi-kernel = { path = "../foi-kernel" }
foi-lane = { path = "../foi-lane" }
