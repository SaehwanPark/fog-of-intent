[package]
name = "foi-gui"
version = "0.1.239"
edition = "2024"
rust-version = "1.96"
license = "MIT"
description = "Presentation-only HTML5/CSS/SVG generator, client state machine, and parity engine for Fog of Intent"

[lints.clippy]
as_conversions = "deny"

[dependencies]
foi-kernel = { path = "../foi-kernel" }
foi-lane = { path = "../foi-lane" }
foi-protocol = { path = "../foi-protocol" }
