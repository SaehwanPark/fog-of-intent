[package]
name = "foi-map"
version = "0.1.238"
edition = "2024"
rust-version = "1.96"
license = "MIT"
description = "Authoritative M9 5v5 multi-lane spatial map topology, structures, and contest simulation for Fog of Intent"

[lints.clippy]
as_conversions = "deny"

[dependencies]
foi-kernel = { path = "../foi-kernel" }
