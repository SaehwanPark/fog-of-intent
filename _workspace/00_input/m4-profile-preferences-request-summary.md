# M4 Profile-Preferences Request Summary

## Requested slice

Expose each scripted profile's fixed baseline preferred intent separately from
the actor-visible threat override.

## Required boundaries

- Return `Stabilize`, `Contest`, or `Yield` from profile metadata only.
- Keep visible RiverSide `Withdraw` selection, candidate generation, scoring,
  host validation, and transition authority unchanged.
- Do not claim a complete risk/personality model, memory, planning horizon,
  communication, or human behavior.

## Evidence target

The cautious, risk-taking, and yielding constructors report baseline
preferences `Stabilize`, `Contest`, and `Yield` respectively, while the
existing visible-threat regression still selects `Withdraw` for cautious.
